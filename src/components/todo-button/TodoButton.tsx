import * as React from 'react';
/**
 * TodoButton should render a clickable button which updates our store of an added action. This component
 * state should be represented by an object that contains actionItem, category, and clicked boolean.
 * 
 * TODO(ibssasimon): Implement TodoButton with component state.
 */
class TodoButton extends React.Component {
  constructor(props: any) {
    super(props);
  }
}

export default TodoButton;
